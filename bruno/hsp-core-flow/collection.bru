docs {
  HSP core API integration flow.

  This collection follows API_DOCUMENTATION.md:
  create order, prepare an available worker, assign worker, worker confirms
  and executes service, customer service confirms payment and closes the order,
  then verifies order, dispatch, execution, and payment records.
}
